# Visual companion sandbox (mockups + server state from brainstorming)
.superpowers/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.eggs/
*.egg-info/
dist/
build/

# Environment / secrets
.env
.env.*

# Per-install config (copy from config.example.yaml on first run)
/config.yaml
!.env.example

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local data (when the project is built out)
data/
logs/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Python (added by /development:bootstrap — net-new entries only;
# duplicates of the section above were deduped before append)
ENV/
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
coverage.json
htmlcov/
.tox/
