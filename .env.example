# Copy to .env (which is gitignored) and fill in.

# Required when classifier.backend == anthropic_api in config.yaml.
# Get a key at https://console.anthropic.com/settings/keys
# This is billed separately from your Claude Code / Max Plan subscription.
ANTHROPIC_API_KEY=sk-ant-...
