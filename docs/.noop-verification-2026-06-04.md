# Noop workflow verification

Throwaway file used to verify that the doc-only noop workflows fire on
doc-path-only PRs and that the real `quality-public.yml` + `codeql.yml`
workflows correctly skip via `paths-ignore`.

Created: 2026-06-04. Safe to delete after the PR is closed.
